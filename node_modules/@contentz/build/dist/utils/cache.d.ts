import { IState } from "../state";
interface Cache {
    version: string;
    [path: string]: string;
}
declare function read(): Promise<Cache>;
declare function check(path: string, content: string | object): Promise<boolean>;
declare function write(state: IState): Promise<void>;
export { write, read, check };
