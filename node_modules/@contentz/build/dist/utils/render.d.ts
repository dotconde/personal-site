import { IState } from "../state";
interface RenderConfiguration {
    state: IState;
}
declare type Rendered = Promise<string>;
declare function render(ui: JSX.Element, configuration: RenderConfiguration): Rendered;
export { render };
