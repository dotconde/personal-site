import { IPath } from "./parse-path";
interface IFile extends IPath {
    content: string;
}
interface IFrontMatterFile extends IPath {
    data: object;
    content: string;
}
declare function readMeta(file: IFile): IFrontMatterFile;
export { readMeta, IFile, IFrontMatterFile };
