interface IPath {
    tmpPath: string;
    filePath: string;
    repositoryPath?: string;
    path: string;
}
interface config {
    isSlide?: boolean;
}
declare function parsePath(path: string, { isSlide }?: config): IPath;
declare function parseRepositoryPath(repository: string, filePath: string): string;
export { parsePath, parseRepositoryPath, IPath };
