import { FunctionComponent } from "react";
interface SlideProps {
    totalSlides: number;
    Component: FunctionComponent<{
        components: any;
    }>;
}
declare function Slide({ Component, totalSlides }: SlideProps): JSX.Element;
export default Slide;
