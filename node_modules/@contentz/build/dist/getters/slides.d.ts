import { IContent, ContentType } from "../definitions";
import { IConfig } from "./config";
interface ISlide extends IContent {
    type: ContentType.Slide;
    published?: boolean;
}
declare type SlidesMap = {
    [path: string]: ISlide;
};
interface ISlides {
    order: string[];
    byPath: SlidesMap;
}
declare function slides(config: IConfig): Promise<ISlides>;
export { slides, ISlides, ISlide };
