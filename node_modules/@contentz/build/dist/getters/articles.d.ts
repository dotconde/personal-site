import { IContent, ContentType } from "../definitions";
import { IConfig } from "./config";
interface ITranslatedTo {
    lang: string;
    path: string;
}
interface IArticle extends IContent {
    type: ContentType.Article;
    next?: {
        title: string;
        description?: string;
        path: string;
    };
    published?: boolean;
    tags?: string[];
    toc?: boolean;
    translated_to?: ITranslatedTo[];
    translated_from?: {
        lang: string;
        title: string;
        path: string;
    };
}
declare type ArticlesMap = {
    [path: string]: IArticle;
};
interface IArticles {
    order: string[];
    byPath: ArticlesMap;
}
declare function articles(config: IConfig): Promise<IArticles>;
export { articles, IArticles, IArticle };
